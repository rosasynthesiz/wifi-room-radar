name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]" websockets
      - name: Unit & system tests
        run: python -m pytest tests -q
      - name: Accuracy gate (sensing regression firewall)
        run: python scripts/check_accuracy.py --quick
